using UnityEngine;

namespace PhantasmicDev.PackageTemplate
{
    public class PackageTemplate : MonoBehaviour
    {
        public bool isCool;

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
